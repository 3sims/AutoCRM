import { Module } from '@nestjs/common'
// Domain: messaging — expand with entity, service, controller, repository
@Module({ imports: [], providers: [], controllers: [], exports: [] })
export class MessagingModule {}
