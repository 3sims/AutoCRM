import { Module } from '@nestjs/common'
// Domain: users — expand with entity, service, controller, repository
@Module({ imports: [], providers: [], controllers: [], exports: [] })
export class UsersModule {}
